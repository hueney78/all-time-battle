"""WebSocket message types (pydantic models).

Phase 3 implementation. All messages: {"type": "...", "payload": {...}}.
Unknown message types are logged and ignored (forward compatibility).
"""

# TODO Phase 3
