# Copy this file to .env and fill in your values.
# Phases 1–4 work without an API key (AI_MODE=mock).

ANTHROPIC_API_KEY=sk-ant-...

# live = real Claude API calls  |  mock = fixture responses (offline dev + tests)
AI_MODE=mock
