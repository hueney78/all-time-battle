# conditions.yaml — curated condition registry.
# The AI may ONLY apply conditions from this list (enforced by schema + validator).
# Adding a condition = add a YAML block; zero Python changes required.
# The condition list is injected into AI prompts automatically.

conditions:
  burning:
    duration: 2
    tick_damage: 2
    cure_tags: [water, soggy]
    debuff: true
    emoji: "🔥"

  soggy:
    duration: 2
    modifiers: {power: -2}
    immunities: [burning]
    debuff: true
    emoji: "💧"

  sticky:
    duration: 2
    modifiers: {speed: -1}
    blocks_free_step: true
    debuff: true
    emoji: "🟢"

  prone:
    duration: 1
    stand_cost: 1
    modifiers: {ac: -1}
    debuff: true
    emoji: "🙃"

  frightened:
    duration: 2
    modifiers: {attack: -1}
    debuff: true
    emoji: "😱"

  embarrassed:
    duration: 2
    modifiers: {attack: -1}
    trigger: "fumble"
    debuff: true
    emoji: "😳"

  # Rage: an attack buff with a defensive downside — not something `cleanse`
  # should strip, so it is intentionally not tagged as a debuff.
  enraged:
    duration: 2
    modifiers: {attack: 1, ac: -1}
    emoji: "😡"

  sparkly:
    duration: 2
    incoming_attack_bonus: 1
    debuff: true
    emoji: "✨"

  hidden:
    duration: 1
    untargetable_melee: true
    ac_bonus_vs_ranged: 2
    emoji: "👻"

  off_balance:
    duration: 1
    ac_penalty_vs_feinter_team: 2
    debuff: true
    emoji: "😵"

  pumped:
    duration: 2
    modifiers: {attack: 1}
    emoji: "💪"

  confused:
    duration: 1
    randomize_targets: true
    debuff: true
    emoji: "🌀"

  shielded:
    duration: 1
    modifiers: {ac: 2}
    emoji: "🛡"

  # Marker for an active Wild Shape. The stat change is applied directly on the
  # character (with clamping) and reverted when this condition expires, so the
  # duration here owns how long a transform lasts.
  transformed:
    duration: 2
    emoji: "🦋"
